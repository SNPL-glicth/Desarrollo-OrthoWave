# Configuración temporal para SQLite (mientras se configura MySQL)
NODE_ENV=development

# Para SQLite no necesitamos configuración de base de datos
# Se usará orto_whave_dev.db en el directorio raíz

# Configuración JWT
JWT_SECRET=orto_whave_jwt_secret_key_desarrollo_2024
JWT_EXPIRES_IN=24h

# Configuración del Servidor
PORT=4000

# Configuración de Email (MailerService) - Configuración de prueba
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=test@ortowhave.com
MAIL_PASS=test_password
MAIL_FROM=noreply@ortowhave.com

# Configuración CORS
CORS_ORIGIN=http://localhost:3000

# Configuración de logs
LOG_LEVEL=info
